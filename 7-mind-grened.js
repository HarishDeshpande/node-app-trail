const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`the sum is: ${num1 + num2}`);
}

// as we are returning the function, it'll run even without assigning it to a variable.
addValues();
